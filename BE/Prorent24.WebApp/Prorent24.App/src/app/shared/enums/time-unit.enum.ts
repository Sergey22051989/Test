export enum TimeUnit {
  empty,
  minutes = "minutes",
  hours = "hours",
  days = "days"
}

export enum TimeUnitExtend {
  empty,
  // empty2,
  //hours = "hours",
  days = "days",
  weeks = "weeks",
  months = "months"
}
